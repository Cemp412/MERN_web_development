const sum = (a, b) => a+b;
const mul = (a,b) => a*b;
const g = 9.8;
const PI = 3.14;

let obj = {
    sum: sum,
    mul: mul,
    g: g,
    PI: PI
}
// module.exports = obj;

//
export const add = (x, y) => x+y;
export const multiply = (x, y) => x*y;
export const z = 8.9;
export const pie = 3.14;